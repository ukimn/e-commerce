import type { NextConfig } from "next";
const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn.dummy.com", "dummyjson.com", "cdn.dummyjson.com", "images.unsplash.com"],
  }
};
export default nextConfig;